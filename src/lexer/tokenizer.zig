// This function should return a type Token in the future, but for now just prints everything out to confirm the parser works
pub fn Tokenizer(str: []const 8) {
    for (str, 0..) |ch, index| {
        
    }
}